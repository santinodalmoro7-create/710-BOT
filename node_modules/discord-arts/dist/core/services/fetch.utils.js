"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.fetchUserData = fetchUserData;
const error_utils_1 = require("../errors/error.utils");
const cross_fetch_1 = __importDefault(require("cross-fetch"));
const fs_1 = require("fs");
const path_1 = require("path");
const BASE_URL = "https://discord-arts.asure.dev/v1/user";
function fetchUserData(userId) {
    return __awaiter(this, void 0, void 0, function* () {
        const packageJsonPath = (0, path_1.join)(__dirname, "..", "..", "..", "package.json");
        const packageJson = JSON.parse((0, fs_1.readFileSync)(packageJsonPath, "utf8"));
        const version = packageJson.version;
        try {
            const response = yield (0, cross_fetch_1.default)(`${BASE_URL}/${userId}`, {
                headers: {
                    "x-darts-version": version,
                },
            });
            const contentType = response.headers.get("content-type");
            if (!(contentType === null || contentType === void 0 ? void 0 : contentType.includes("application/json"))) {
                if (contentType === null || contentType === void 0 ? void 0 : contentType.includes("text/html")) {
                    throw new error_utils_1.DiscordArtsError("API is currently down, try again later");
                }
                else {
                    throw new error_utils_1.DiscordArtsError("Invalid response format. Expected JSON, but received: " +
                        contentType);
                }
            }
            const json = yield response.json();
            if (json.error || !response.ok) {
                throw new error_utils_1.DiscordArtsError(json === null || json === void 0 ? void 0 : json.message);
            }
            return json.data;
        }
        catch (error) {
            throw new error_utils_1.DiscordArtsError((error === null || error === void 0 ? void 0 : error.message) || "API is currently down, try again later");
        }
    });
}
