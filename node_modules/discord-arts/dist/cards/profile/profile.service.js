"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.genPng = genPng;
const canvas_1 = require("@napi-rs/canvas");
const path_1 = __importDefault(require("path"));
const profile_utils_1 = require("./utils/profile.utils");
const assets_1 = require("../../constants/assets");
function registerFonts(selectedFont = 'HELVETICA', customFont) {
    if (customFont) {
        const pathWithoutExtension = customFont.replace(/\.[^/.]+$/, '');
        const fontName = path_1.default.basename(pathWithoutExtension, 'ttf');
        canvas_1.GlobalFonts.registerFromPath(customFont, fontName);
        return fontName;
    }
    const font = assets_1.FONTS[selectedFont];
    canvas_1.GlobalFonts.registerFromPath(`./public/fonts/${font.paths.regular}`, font.regular);
    canvas_1.GlobalFonts.registerFromPath(`./public/fonts/${font.paths.bold}`, font.bold);
    return assets_1.FONTS[selectedFont];
}
function genPng(data, options) {
    return __awaiter(this, void 0, void 0, function* () {
        var _a, _b, _c, _d;
        registerFonts(options === null || options === void 0 ? void 0 : options.font, options === null || options === void 0 ? void 0 : options.customFont);
        if (options === null || options === void 0 ? void 0 : options.customFont) {
            const pathWithoutExtension = options.customFont.replace(/\.[^/.]+$/, '');
            const fontName = path_1.default.basename(pathWithoutExtension, 'ttf');
            options.customFont = fontName;
        }
        const { basicInfo, assets } = data;
        const canvas = (0, canvas_1.createCanvas)(885, 303);
        const ctx = canvas.getContext('2d');
        const userAvatar = ((_a = assets.avatarURL) !== null && _a !== void 0 ? _a : assets.defaultAvatarURL) + '?size=512';
        const userBanner = assets.bannerURL ? assets.bannerURL + '?size=512' : null;
        const badges = yield (0, profile_utils_1.getBadges)(data, options);
        if (options === null || options === void 0 ? void 0 : options.removeBorder)
            ctx.roundRect(9, 9, 867, 285, [26]);
        else
            ctx.roundRect(0, 0, 885, 303, [34]);
        ctx.clip();
        const cardBase = yield (0, profile_utils_1.genBase)(options, userAvatar, userBanner);
        ctx.drawImage(cardBase, 0, 0);
        const cardFrame = yield (0, profile_utils_1.genFrame)(badges, options);
        ctx.drawImage(cardFrame, 0, 0);
        const cardTextAndAvatar = yield (0, profile_utils_1.genTextAndAvatar)(data, options, userAvatar);
        const textAvatarShadow = (0, profile_utils_1.addShadow)(cardTextAndAvatar);
        ctx.drawImage(textAvatarShadow, 0, 0);
        ctx.drawImage(cardTextAndAvatar, 0, 0);
        if (!(options === null || options === void 0 ? void 0 : options.disableProfileTheme) &&
            ((_b = data === null || data === void 0 ? void 0 : data.decoration) === null || _b === void 0 ? void 0 : _b.profileColors) &&
            typeof (options === null || options === void 0 ? void 0 : options.borderColor) === 'undefined') {
            options = options || {};
            options.borderColor = (_c = data === null || data === void 0 ? void 0 : data.decoration) === null || _c === void 0 ? void 0 : _c.profileColors;
            if (!options.borderAllign) {
                options.borderAllign = 'vertical';
            }
        }
        if ((typeof (options === null || options === void 0 ? void 0 : options.borderColor) === 'string' && options.borderColor) ||
            (Array.isArray(options === null || options === void 0 ? void 0 : options.borderColor) && options.borderColor.length > 0)) {
            const border = yield (0, profile_utils_1.genBorder)(options);
            ctx.drawImage(border, 0, 0);
        }
        if (basicInfo === null || basicInfo === void 0 ? void 0 : basicInfo.bot) {
            const botVerifBadge = yield (0, profile_utils_1.genBotVerifBadge)(data, options);
            const shadowVerifBadge = (0, profile_utils_1.addShadow)(botVerifBadge);
            ctx.drawImage(shadowVerifBadge, 0, 0);
            ctx.drawImage(botVerifBadge, 0, 0);
        }
        if (!(options === null || options === void 0 ? void 0 : options.removeBadges)) {
            const cardBadges = yield (0, profile_utils_1.genBadges)(badges);
            const badgesShadow = (0, profile_utils_1.addShadow)(cardBadges);
            ctx.drawImage(badgesShadow, 0, 0);
            ctx.drawImage(cardBadges, 0, 0);
        }
        if (options === null || options === void 0 ? void 0 : options.rankData) {
            const xpBar = (0, profile_utils_1.genXpBar)(options);
            ctx.drawImage(xpBar, 0, 0);
        }
        if (!(options === null || options === void 0 ? void 0 : options.removeAvatarFrame) && ((_d = data === null || data === void 0 ? void 0 : data.decoration) === null || _d === void 0 ? void 0 : _d.avatarFrame)) {
            const avatarFrame = yield (0, profile_utils_1.genAvatarFrame)(data, options);
            ctx.drawImage(avatarFrame, 0, 0);
        }
        return canvas.toBuffer('image/png');
    });
}
