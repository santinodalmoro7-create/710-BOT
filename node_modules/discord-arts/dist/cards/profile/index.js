"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const error_utils_1 = require("../../core/errors/error.utils");
const fetch_utils_1 = require("../../core/services/fetch.utils");
const profile_service_1 = require("./profile.service");
function Profile(userId, options) {
    return __awaiter(this, void 0, void 0, function* () {
        if (!options)
            options = {};
        if (!userId || typeof userId !== 'string')
            throw new error_utils_1.DiscordArtsError(`TypeError: Invalid argument for profileImage()\nExpected string userId, got ${typeof userId === 'undefined' || !userId ? 'undefined' : typeof userId}`);
        const data = yield (0, fetch_utils_1.fetchUserData)(userId);
        try {
            const buffer = yield (0, profile_service_1.genPng)(data, options);
            return buffer;
        }
        catch (error) {
            if (error instanceof Error && error.message.includes('source rejected')) {
                throw new error_utils_1.DiscordArtsError(`Error loading user assets, try again later`);
            }
            throw new error_utils_1.DiscordArtsError(error === null || error === void 0 ? void 0 : error.message);
        }
    });
}
exports.default = Profile;
